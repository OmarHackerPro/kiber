PROJECT_NAME=news.avild.com
DEBUG=false
HOST=127.0.0.1
PORT=8000

# Local dev (matches docker-compose.yml defaults)
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/avild_news

# Cloud (Supabase, Neon, RDS, etc.) — replace when available
# DATABASE_URL=postgresql+asyncpg://user:password@host:5432/dbname
